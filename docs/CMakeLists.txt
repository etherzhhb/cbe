if (LLVM_ENABLE_SPHINX)
  if (SPHINX_FOUND)
    include(AddSphinxTarget)
    if (${SPHINX_OUTPUT_HTML})
      add_sphinx_target(html cbe)
    endif()
    if (${SPHINX_OUTPUT_MAN})
      add_sphinx_target(man cbe)
    endif()
  endif()
endif()